---
---

v2.0.0 has already been applied to every `@reaatech/hybrid-rag-*` package via
`changeset version` (multi-vector-DB adapter architecture; BREAKING: `qdrantUrl` /
`qdrantApiKey` replaced by `vectorStore` config). This empty changeset records that
the current source changes are accounted for by that release and require no further
version bump, keeping `changeset status` clean. Remove it before authoring the next
release's changesets.
