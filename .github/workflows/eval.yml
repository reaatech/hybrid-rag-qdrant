name: Evaluation

on:
  pull_request:
    branches: [main]
    paths:
      - 'packages/**'
      - 'datasets/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: 22

jobs:
  evaluate:
    runs-on: ubuntu-latest
    services:
      qdrant:
        image: qdrant/qdrant:latest
        ports:
          - 6333:6333

    steps:
      - name: Checkout base
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: base

      - name: Checkout PR
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          path: pr

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies (base)
        working-directory: base
        run: pnpm install --frozen-lockfile

      - name: Build (base)
        working-directory: base
        run: pnpm build

      - name: Run evaluation (base)
        working-directory: base
        run: node packages/hybrid-rag-cli/dist/index.js evaluate --dataset datasets/examples/sample.jsonl --output base-eval.json
        env:
          QDRANT_URL: http://localhost:6333
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Install dependencies (PR)
        working-directory: pr
        run: pnpm install --frozen-lockfile

      - name: Build (PR)
        working-directory: pr
        run: pnpm build

      - name: Run evaluation (PR)
        working-directory: pr
        run: node packages/hybrid-rag-cli/dist/index.js evaluate --dataset datasets/examples/sample.jsonl --output pr-eval.json
        env:
          QDRANT_URL: http://localhost:6333
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Compare results
        run: |
          echo "## Evaluation Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Base Results" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          cat base/base-eval.json >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### PR Results" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          cat pr/pr-eval.json >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Upload base results
        uses: actions/upload-artifact@v4
        with:
          name: base-eval
          path: base/base-eval.json

      - name: Upload PR results
        uses: actions/upload-artifact@v4
        with:
          name: pr-eval
          path: pr/pr-eval.json
